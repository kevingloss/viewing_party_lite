<h1>Viewing Party Lite</h1>
<h2>Discover Movies</h2>

<%= button_to "Top Rated Movies", user_movies_path(@user), method: :get, params: {filter: 'top_rated'} %>

<p>OR</p>

<%= form_with url: user_movies_path(@user), method: :get, local: true do |f| %>
    <%= f.label :filter, "Search by Title" %>
    <%= f.text_field :filter %>
    <%= f.submit :search%>
<% end %>